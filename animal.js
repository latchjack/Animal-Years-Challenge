const animalYears = 15;

// This grabs the input box
var theButton = document.getElementById(theButton);

theButton.addEventListener('click',);

// The below code works out the cat years x user years
// I need to make the line below work when the button is
// clicked. I need to assign it to a click event on the button.
var userYears = parseInt(document.getElementById(inYears));

var newYears = animalYears * userYears;

console.log(newYears);